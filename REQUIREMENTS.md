# Core dependencies (installed automatically with pip install -e .)
numpy>=1.24
numba>=0.57

# Optional — PyTorch drop-in replacement (torch_compat.py)
# pip install torch

# Optional — GPT-2 demo (examples/demo_gpt2.py, detmatmul/gpt2.py)
# pip install transformers tiktoken

# Optional — REST API (detmatmul/api/server.py)
# pip install fastapi uvicorn

# Development / testing
# pip install pytest
